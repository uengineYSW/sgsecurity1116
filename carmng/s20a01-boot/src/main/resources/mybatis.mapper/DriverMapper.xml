
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.posco.carmng.s20a01.domain.driver.mybatis.DriverMapper">
    
    <!-- 예시 -->
    <!-- <select id="selectDriverList" resultType="DriverMybatisEntity">
        SELECT * FROM Driver_table
    </select>
    <select id="selectDriver" resultType="DriverMybatisEntity">
        SELECT * FROM Driver_table WHERE driverEmpNo = #{id}
    </select> -->
</mapper>