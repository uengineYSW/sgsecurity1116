package com.posco.carmng.s20a01.domain.driver.mybatis;

import java.util.List;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface DriverMapper {
    // 예시
    // List<DriverMybatisEntity> selectDriverList();
    // DriverMybatisEntity selectDriver(String id);
}
